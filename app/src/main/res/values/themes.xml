<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Session2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
    </style>

    <style name="Theme.Session2" parent="Base.Theme.Session2" />

    <style name="NewCheckBox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:state_checked">true</item>
        <item name="android:drawableBottom">@drawable/chb_blue_selected</item>
    </style>

</resources>